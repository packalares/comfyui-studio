Batch-install flow for the hardcoded "essential" model bundle (SDXL base +
refiner + common VAE + upscalers). See `essentialModels.data.ts` for the
list and `essentialModels.service.ts` for the orchestration.
