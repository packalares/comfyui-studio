// rgthree "Fast Groups Muter" class type — split out so the constant has a
// single source of truth across the FGM resolver and any future probes that
// need to detect the muter (e.g. an "is this template FGM-driven?" hint
// surfaced in the template-bundle response).

export const FGM_CLASS_TYPE = 'Fast Groups Muter (rgthree)';
