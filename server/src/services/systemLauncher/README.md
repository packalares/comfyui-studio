System-controller services: pip/HF/GH config writes, network status checks,
desktop-bridge "open path" calls. Named `systemLauncher/` to avoid collision
with `routes/system.routes.ts` (studio's own system-info endpoints).
