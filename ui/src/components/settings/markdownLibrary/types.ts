// Shared types for the generic MarkdownLibrarySection + MarkdownEditorModal.
// Souls, Skills, and Commands all follow this shape for their list items.

export interface LibraryItem {
  name: string;
  description: string;
}
